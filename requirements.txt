# core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0           # parquet for data/processed
scikit-learn>=1.3
joblib>=1.3

# data validation (stage 3)
pandera>=0.20,<0.24

# ingestion (stage 2)
requests>=2.31

# serving (stage 6)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# monitoring + CT (stages 7-8)
pymongo[srv]>=4.6      # [srv] pulls dnspython, required for mongodb+srv:// URIs

# dev / CI (stage 4)
pytest>=8.0
httpx>=0.27            # FastAPI TestClient
ruff>=0.4
